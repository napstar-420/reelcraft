name: CI

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: reefcraft
          POSTGRES_PASSWORD: reefcraft
          POSTGRES_DB: reefcraft
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U reefcraft -d reefcraft"
          --health-interval 2s
          --health-timeout 5s
          --health-retries 20
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @reefcraft/shared build
      - run: pnpm typecheck
      - run: pnpm --filter @reefcraft/api typecheck
      - run: pnpm lint
      - run: pnpm format:check
      - run: pnpm --filter @reefcraft/shared test
      - run: pnpm --filter @reefcraft/api test
      - run: pnpm --filter @reefcraft/api test:e2e
        env:
          TEST_DATABASE_URL: postgres://reefcraft:reefcraft@localhost:5432/reefcraft
